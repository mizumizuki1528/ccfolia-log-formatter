# フィードバックまとめ

## 受領日: 2026/05/14

---

### 不具合・改善要望

| # | 内容 | 優先度 | 対応状況 |
|---|------|--------|----------|
| 1 | 色設定パネルで発言者が多すぎると表示しきれないものがある（パネルの高さ制限？スクロール問題？） | 高 | 対応済み（見落としだったが、UX改善として2行構成＋発言数ソートを実装） |
| 2 | 名前の色が白っぽいキャラだと背景に溶けて見づらい。エクスポート画面や編集画面のダークモード、もしくは文字の縁取りがあると良い | 中 | 対応済み（自動縁取り＋手動設定＋背景色変更機能） |
| 3 | CCBの成功・失敗で色がつくと見やすい（好みの問題） | 低 | 対応済み（エクスポートにもダイスハイライト反映） |
| 4 | 差分登録用に個別に画像を差し替える機能（発言単位でアイコンを変更したい） | 中 | 未対応 |
| 5 | 秘匿等を操作しやすいように一括で並べ替える機能（タブ単位でまとめて移動・整理） | 中 | 未対応 |
| 6 | エクスポートのヘッダーにカラー設定（1色 or 2色グラデーション）を追加したい | 中 | 対応済み（縦グラデーション＋テキスト色設定） |
| 7 | ビューア画面でのタイトル変更（KP選択画面以外でもタイトルを編集したい） | 低 | 未対応 |
| 8 | 縁取りのぼかしを抑える or ぼかし無しにする（縁取りありとなしが並ぶと浮いて見える） | 低 | 未対応 |

---

### 好評だった点

| # | 内容 |
|---|------|
| 1 | 雑談が横に出るのが凄く見やすい |
| 2 | アイコン設定できる（神） |
| 3 | 角丸でかわいい |
| 4 | KPとかを地文設定すると丸枠なくなり視認性最高 |
| 5 | 横に雑談が入るのが好き |
| 6 | 秘匿のところ点線でわかりやすい |
| 7 | 無印（ココフォリア標準出力）のものも問題なく読めた |

---

### 対応方針メモ

**#1 色設定パネルのスクロール問題**
- ~~カラーパネルの `#color-list` に `overflow-y: auto` は設定済みだが、パネル自体の高さが画面に収まっていない可能性~~
- → 見落としだったが、UX改善として対応
- 発言数順ソート＋バッジ表示＋2行構成レイアウトを実装（テスト版で検証済み）
- 本番反映待ち

**#2 白っぽい色の視認性**
- 縁取り（text-shadow）が無難
- 加えて背景色もつけられるようにするとよい
- 案A: 発言者名に `text-shadow` で薄い縁取りを追加
- 案B: エクスポート/ビューアにダークモード切替を復活
- 案C: 明るすぎる色を自動検出して背景を暗くする
- → まずは縁取り＋背景色設定の組み合わせで対応

**#3 CCBの成功・失敗の色分け**
- ダイスロール結果のハイライト機能は実装済み（成功=緑、失敗=赤）
- エクスポート時にも反映されるよう対応済み

**#4 差分登録用に個別に画像を差し替える機能**
- 現状はカラーパネルで発言者単位のアイコン設定のみ
- 要望：同じ発言者でもシーンによって別のアイコン（表情差分等）を使いたい
- 案A: 編集モーダルに「この発言のアイコンを変更」オプション追加
- 案B: 発言者に複数アイコンを登録し、発言ごとに選択

**#5 秘匿等を一括で並べ替える機能**
- 現状はドラッグ＆ドロップで1発言ずつ移動のみ
- 要望：秘匿タブの発言をまとめて特定の位置に移動したい
- 案A: タブ設定パネルに「このタブの発言をまとめて末尾/先頭に移動」ボタン
- 案B: 複数選択→一括移動機能
- 案C: タブごとにソート（タブ内の発言を時系列順に並べ直す）

**#6 エクスポートヘッダーのカラー設定**
- 要望：出力HTMLのヘッダー部分に背景色を設定したい
- カラーピッカー2つ（色1、色2）
- 2色設定 → linear-gradientでグラデーション
- 1色のみ → 単色背景
- テキスト色もカラーピッカーで指定可能
- 設定場所：タブ設定パネル or 専用の「出力設定」セクション

---
